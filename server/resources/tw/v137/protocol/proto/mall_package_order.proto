syntax = "proto3";

package proto;
import public "public.proto";
import "mall_gem_order.proto";

message MallPackageOrder {
  bytes NextPackage = 2047;

  oneof Resp {
    .proto.ChangeInfo Change = 1;
    .proto.OrderInfo Order = 2;
  }
}
